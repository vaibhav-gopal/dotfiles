# Template .env file : put the real .env in the same directory
# Need the following defaults for building / switching via justfile
# NIXTYPE : the nix type, one of the top level directories in dotfiles/nix (used for validation / ensuring you don't build the wrong nix type configuration)
# NIXBUILDRECIPE : the literal justfile recipe name for the systems build command
# NIXCONFIG : the top-level configuration name in flake.nix to activate
# NIXCONFIG_VERSION : the lts nixpkgs version in which to use, to get the approriate nixos package or update the channel
# NIXCONFIG_USERNAME : the username for the nix config, to get home manager options using `nix eval`
NIXTYPE=nix-darwin
NIXBUILDRECIPE=darwin_build
NIXCONFIG=vgmacbook
NIXCONFIG_VERSION=25.11
NIXCONFIG_USERNAME=vaibhav
